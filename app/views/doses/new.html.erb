<h2>Create a new dose to <%= @cocktail.name %></h2>

<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient %>
  <%= f.submit "add this dose", class: "btn btn-primary" %>
<% end %>
<div>
<%= link_to '<< Back', cocktails_path%>
</div>
